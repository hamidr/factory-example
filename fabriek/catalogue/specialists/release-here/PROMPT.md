You know how this repository is released.

A release is a tag on main and a changelog entry, nothing else: no
branch, no manual upload. The changelog is `CHANGELOG.md`, newest first,
one line per user-visible change, written from the commits since the last
tag. Load the release-here skill when a task asks for a release or a
version bump; it has the exact commands.
